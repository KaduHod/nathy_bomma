<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('partials/head', { title: title }) %>
</head>
<body>
    <main class="main main-solo">
        <header class="topbar">
            <div style="min-width:0;">
                <div class="eyebrow">Administrador</div>
                <h1>Clientes</h1>
                <p class="view-intro">Gerencie os clientes atendidos pela agência.</p>
            </div>
            <div class="topbar-meta">
                <div class="tchip">
                    <span class="n" id="totalCount">—</span>
                    <span class="l">Total</span>
                </div>
                <a href="/" class="btn-ghost">← Voltar ao dashboard</a>
                <button class="btn-primary" id="btnNovo">+ Novo cliente</button>
            </div>
        </header>

        <div id="loading" class="state"><div class="spin"></div>Carregando clientes…</div>
        <div id="error" class="state--error" hidden></div>

        <section id="content" hidden>
            <div class="panel">
                <div class="panel-head">
                    <span class="panel-title">Clientes</span>
                    <div class="searchbox">
                        <input type="text" id="busca" placeholder="Buscar por nome…" />
                    </div>
                </div>
                <div class="tbl-wrap">
                    <table>
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th style="text-align:right;">Ações</th>
                            </tr>
                        </thead>
                        <tbody id="tbody"></tbody>
                    </table>
                </div>
                <div id="emptyState" class="empty-state" hidden>Nenhum cliente encontrado.</div>
            </div>
        </section>
    </main>

    <!-- Modal criar/editar -->
    <div class="modal-overlay" id="modalOverlay">
        <div class="modal">
            <div class="modal-head">
                <h3 id="modalTitle">Novo cliente</h3>
                <button class="modal-close" id="modalClose">✕</button>
            </div>
            <form id="form">
                <input type="hidden" id="fId" />
                <div class="form-group">
                    <label for="fNome">Nome *</label>
                    <input type="text" id="fNome" required />
                </div>
                <div class="form-error" id="formError"></div>
                <div class="modal-actions">
                    <button type="button" class="btn-ghost" id="btnCancelar">Cancelar</button>
                    <button type="submit" class="btn-primary" id="btnSalvar">Salvar</button>
                </div>
            </form>
        </div>
    </div>

    <div class="toast" id="toast"></div>

    <script src="/cliente.js"></script>
</body>
</html>
